import {withRouter} from 'react-router';
import {Modal} from '../components';

const Home = (props) => {
    return (
        <>
        <Modal></Modal>
        </>
    )
}

export default withRouter(Home);


